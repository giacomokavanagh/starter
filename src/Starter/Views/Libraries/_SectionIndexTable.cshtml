@model IEnumerable<Starter.Models.Section>

<div class='panel-group'>
    <div class='panel panel-default'>
        <div class='panel-heading'>
            <h3 class='panel-title'>
                <a data-toggle='collapse' id='Toggle' href='#collapseSections'>Sections</a>
            </h3>
        </div>
        @if (Model.Any())
        {
            <div id='collapseSections' class='panel-collapse collapsed collapse in'>
                <div class='panel-body'>
                    <table id="3ColumnDataTable" class="table table-striped">
                        <thead id="greyTableheader">
                            <tr>
                                <th>
                                    ID
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Name)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Description)
                                </th>
                            </tr>
                        </thead>
                        <tbody id="RightClickTable">
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        <p class="TableParagraph">@item.SectionID</p>
                                    </td>
                                    <td>
                                        <p class="TableParagraph">
                                            <a asp-controller="Sections" asp-action="Details" asp-route-id="@item.SectionID" class="Actions">@item.Name</a>
                                        </p>
                                    </td>
                                    <td>
                                        <p class="TableParagraph">
                                            @Html.DisplayFor(modelItem => item.Description)
                                        </p>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        }

        else
        {
            <div id='collapseSections' class='panel-collapse collapsed collapse in'>
                <div class='panel-body'>
                    <p>
                        There are currently no sections
                    </p>
                </div>
            </div>
        }

    </div>
</div>

<ul id="contextMenu" class="dropdown-menu" role="menu" style="display:none">
    <li><a tabindex="-1" href="#" name="Sections">Edit</a></li>
    <li><a tabindex="-1" href="#" name="Sections">Details</a></li>
    <li><a tabindex="-1" href="#" name="Sections">Delete</a></li>
</ul>